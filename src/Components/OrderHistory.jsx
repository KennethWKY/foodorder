import { doc, setDoc, onSnapshot, collection } from "firebase/firestore";

export default function OrderHistory({ db }) {
  //   onSnapshot(collection(db, "order"), (snapshot) =>
  //     snapshot.docs.map((doc) => doc.data())
  //   );
  return <></>;
}
